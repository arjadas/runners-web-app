#spring.h2.console.enabled=true
#spring.datasource.generate-unique-name=false
#spring.datasource.name=runners
#spring.datasource.url=jdbc:postgresql://localhost:5432/runners
#spring.datasource.username=
#spring.datasource.password=
#
#above properties are already mentioned in compose.yml

# schema file only gets picked up by embeded databases by default, so doesn't get picked up automatically with mysql or
# postgres, so needs to tell them that we need to run the schema file
spring.sql.init.mode=always